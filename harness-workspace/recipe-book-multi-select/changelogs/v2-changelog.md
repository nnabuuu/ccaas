# v2 Changelog

## Changes
- Replaced hardcoded `rgba(52, 199, 123, 0.2)` in flash-green keyframes with `var(--success-bg)` design token
- Increased picker dropdown max-height from `280px` to `min(340px, 50vh)` to prevent staging area from pushing list items outside viewport

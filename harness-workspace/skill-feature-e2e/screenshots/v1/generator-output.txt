## Summary

All 10 tasks completed. Here's what was done:

### Layer 1: Code Review Fixes (4 items)
1. **Guard decorator order** — Fixed 6 endpoints across `sessions.controller.ts` (3) and `conversations-alias.controller.ts` (3). `@UseGuards(TenantGuard)` now correctly placed above `@OptionalAuth()` so auth runs first.
2. **Dead code** — Removed unused `isOptionalAuth` variable and `IS_OPTIONAL_AUTH_KEY` import from `skill-permission.guard.ts`
3. **Toast error detail** — `SkillPanel.tsx` catch block now shows `err.message` instead of generic "请重试"

### Layer 2: Tests (4 files, 13 new tests)
- `skills.service.toggle.spec.ts` — **NEW**: 3 tests (toggle on→off, off→on, NotFoundException)
- `skill-management.service.spec.ts` — **EXTENDED**: 3 `loadEnabledSkills` tests (filter enabled, all disabled, slug filter)
- `useSkills.test.ts` — **NEW**: 4 tests (fetch on mount, PATCH toggle, error re-throw, search filter)
- `SkillPanel.test.tsx` — **EXTENDED**: 3 toast tests + fixed 1 existing test for auth pre-check

### Verification Results
- Backend typecheck: PASS
- Chat-interface typecheck: PASS  
- Backend tests: 17 passed
- React SDK tests: 4 passed
- Chat-interface tests: 18 passed
- Grep checks: All 3 PASS

{
  "name": "@kedge-agentic/workflow-client",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-free HTTP client for pushing ontology events to the platform Workflow engine. Solution-side outbox workers wrap this with their own persistence + retry.",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  }
}

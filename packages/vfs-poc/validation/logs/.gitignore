# Validation logs are intentionally checked in.
# They are the reproducible primary record of P0 validation runs — see
# packages/vfs-poc/docs/VALIDATION_REPORT.md for the human-readable summary.
# Overrides root .gitignore's `*.log` exclusion.
!*.log

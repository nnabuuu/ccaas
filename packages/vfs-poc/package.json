{
  "name": "@kedge-agentic/vfs-poc",
  "private": true,
  "version": "0.0.2",
  "description": "Design + validation archive for the agentfs overlay + just-bash sandbox work. Production code lives in packages/backend/src/sessions/{workspace,sandbox}/. See README.md.",
  "scripts": {
    "build-agentfs-fix": "bash scripts/build-agentfs-fix.sh"
  }
}

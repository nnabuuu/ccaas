{
  "name": "@kedge-agentic/rest-adapter-bridge",
  "version": "1.0.0",
  "description": "Stdio MCP bridge that forwards tool calls to REST APIs via RestAdapterConfig",
  "type": "module",
  "bin": {
    "rest-adapter-bridge": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-vm-modules ../../../node_modules/.bin/jest --no-coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  }
}

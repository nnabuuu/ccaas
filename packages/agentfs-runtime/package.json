{
  "name": "@kedge-agentic/agentfs-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-free runtime for the agentfs overlay layer: BaseMaterializer + ContentSource port. Backends provide their own ContentSource adapter (e.g. TypeORM) and wire the pure class into their DI container.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  }
}

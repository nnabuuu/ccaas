PORT=3007
CORS_ORIGIN=http://localhost:5283
NODE_ENV=development
LLM_API_KEY=your-api-key-here
LLM_MODEL=deepseek-v4-flash
LLM_BASE_URL=https://api.deepseek.com
LLM_VISION_API_KEY=your-dashscope-api-key-here
LLM_VISION_MODEL=qwen-vl-plus
LLM_VISION_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# ccaas proxy ──────────────────────────────────────────────────────────
# Bearer key + upstream URL for the ccaas-chat / ccaas-project proxies.
CCAAS_API_KEY=
CCAAS_URL=http://localhost:3001

# URL ccaas should call back into live-lesson for artifact CRUD (the
# `sourceUrl` field of attach-workspace-source). Set when ccaas lives on
# a different host than this backend (e.g. docker-compose, k8s). Defaults
# to BACKEND_URL, then http://localhost:3007 if neither is set.
# LIVE_LESSON_PUBLIC_URL=http://live-lesson.internal:3007
